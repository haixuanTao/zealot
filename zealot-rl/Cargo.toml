[package]
name = "zealot-rl"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Data-parallel PPO minibatch gradient accumulation (the CPU update bottleneck).
rayon = "1"
